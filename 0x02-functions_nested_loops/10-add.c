#include "main.h"
/**
 * add - function add two numbers
 * @a: parameters to have
 * @b: the second parameter
 * Return: return the result
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
