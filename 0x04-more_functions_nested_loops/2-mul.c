#include "main.h"
/**
 * mul - function that multiply two integers
 * @a: takes a number to be multolied
 * @b: takes a number
 * Return: the result of multiplying two numbers
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
